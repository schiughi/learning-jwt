ja:
  activemodel:
    models:
      authenticate_parovider_command: ユーザー認証コマンド
    attributes:
      authenticate_parovider_command:
        auth: 認証情報
        token: トークン
    errors:
      models:
        decode_authentication_command:
          attributes:
            auth:
              invalid: 認証情報が不足しています