ja:
  activemodel:
    models:
      authenticate_provider_command: ユーザー認証コマンド
    attributes:
      authenticate_provider_command:
        auth: 認証情報
        token: トークン
    errors:
      models:
        authenticate_provider_command:
          attributes:
            auth:
              invalid: が不足しています